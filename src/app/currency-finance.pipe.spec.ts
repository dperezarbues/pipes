import { CurrencyFinancePipe } from './currency-finance.pipe';

describe('CurrencyFinancePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFinancePipe();
    expect(pipe).toBeTruthy();
  });
});
